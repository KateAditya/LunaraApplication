const axios = require("axios"); const fs = require("fs"); const FormData = require("form-data"); async function run() { fs.writeFileSync("test.jpg", Buffer.alloc(100)); const form = new FormData(); form.append("name", "Test Venue"); form.append("coverImage", fs.createReadStream("test.jpg")); try { const res = await axios.post("http://localhost:5000/api/venues", form, { headers: { ...form.getHeaders() }}); console.log(res.data); } catch (e) { console.error(e.response ? e.response.data : e.message); } } run();
